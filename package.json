{
  "name": "personal-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --sourcemap --minify --outfile=dist/main.js",
    "dev": "esbuild src/main.ts --bundle --sourcemap --outfile=dist/main.js --watch",
    "preview": "npx serve ."
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}


